import { NumberPowerPipe } from './number-power.pipe';

describe('NumberPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
